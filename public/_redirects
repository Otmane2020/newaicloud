# Google Shopping feed proxy for catalogoptimize.com
/shoppingfeed/* https://xkrrcusvmxifbiejxker.supabase.co/functions/v1/shopping-feed/shoppingfeed/:splat 200!

# sitemap.xml is served directly from public/sitemap.xml.
# Keeping it static avoids a dependency on the Supabase Edge Function for SEO crawling.

# SPA fallback - must be last
/* /index.html 200
