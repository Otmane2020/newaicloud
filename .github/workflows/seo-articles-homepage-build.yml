name: SEO articles homepage build check

on:
  push:
    branches: [main]
    paths:
      - '.github/workflows/seo-articles-homepage-build.yml'

permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
      - uses: actions/setup-node@v4
        with:
          node-version: '20'
      - name: Install dependencies
        run: npm install --legacy-peer-deps
      - name: Build
        run: npm run build
      - name: Cleanup workflow
        if: success()
        run: |
          git config user.name "Otmane2020"
          git config user.email "61626050+Otmane2020@users.noreply.github.com"
          rm .github/workflows/seo-articles-homepage-build.yml
          git add .github/workflows/seo-articles-homepage-build.yml
          git commit -m "chore: remove SEO articles homepage build check"
          git push origin main
